# Shared Agent thread context repair

The operator reported that Edison could not read their discussion with xiaobai in the same Matrix room. The original room history contained xiaobai's answers, but Hagency's conversation archive contained the human questions and omitted Agent replies. This was an intake bug, not a Matrix thread visibility restriction.

`sendAsAgentContent` remembers a successfully delivered event ID to prevent routing loops. `onRoomMessage` used that same remembered ID to return before background archival. Dedicated private-chat devices also discarded their own messages before bridge intake. Commit `adf3294` lets authenticated Agent echoes reach the existing admitted background path and lets a dedicated device pass its own reply through that path. Existing room/membership/promotion checks remain enforced; Agent replies never wake a worker even when they mention another Agent. Activity updates stay excluded. Room/event deduplication prevents duplicate context rows.

Three new bridge regressions and one own-device regression reproduce the original failures. After the fix, the six relevant files passed 79 tests, including direct backend admission, conversation pagination/positions, Appservice intake and router regressions. ESLint, diff-check and exact spec bindings passed. Native agent-spec boundary passes but its 12 Node scenarios remain Skip; no lifecycle pass is claimed. An initial fixture accidentally used a resolved filesystem path named `:memory:`; the fixture now uses a unique temporary router database, its test-created file was removed, and failed output is retained.

The live bridge restarted on the existing outbound source at 2026-09-09T02:16:51Z, after all 53 existing dispatches completed. An earlier restart preflight refused while a user turn was still running, and made no service changes. Backend, Palpo and existing identities were preserved. No new private-chat startup warning appeared.

Historical repair read actual Matrix history in the shared Edison/xiaobai room, verified current participants, and used the existing authenticated conversation archive API. It retained original event IDs, sender identities, timestamps and thread roots. It excluded activity and all pre-promotion private history. It did not replay human requests, reset successful positions or edit completed tasks. Initial recovery added 16 missing messages; a follow-up added two replies that finished during deployment. Final comparison found all 28 eligible messages present, with no remaining omissions. Existing human messages replay idempotently.

A separate local proof copied the live router database and used the real dispatch capability/read_conversation implementation. Edison's next frozen range included three independently checked original xiaobai answers, with 19 readable parts. This is actual tool/context verification against a copy, not a new live model turn. No Matrix messages were sent by the verification. Normal future replies depend on subsequent authenticated sync delivery, with durable retry on archival errors.

Protected operational evidence is under `/Users/yuechen/Library/Caches/palpo-admin-e2e/2026-09-06`: shared-thread-discussion-plan.json, shared-thread-discussion-recovered.json and prior copies, shared-thread-discussion-deployment.json, shared-thread-discussion-read-proof.json, plus the original-source backup. Logs are `/tmp/hagency-agent-discussion-before.log`, `/tmp/hagency-agent-discussion-after-isolated.log`, `/tmp/hagency-agent-discussion-regression.log`, and `/tmp/hagency-agent-discussion-lifecycle.json`.

The repair preserves the existing unread-since-last-delivery scope; it does not grant arbitrary access to other rooms or separate DMs, and it does not change a completed turn's frozen context retroactively. The user should mention Edison again in this shared room for a fresh turn.
