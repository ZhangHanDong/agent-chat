name: hagency-services-team

services:
  backend:
    image: "${HAGENCY_IMAGE:-hagency-services:local}"
    build:
      context: ..
      dockerfile: services/Dockerfile
    command: ["node", "backend-v2.js"]
    restart: unless-stopped
    network_mode: host
    read_only: true
    tmpfs: [/tmp]
    env_file:
      - ${HAGENCY_ENV_FILE:-../.env}
    environment:
      NODE_ENV: production
      HAGENCY_RUNTIME_DIR: /var/lib/hagency
    volumes:
      - hagency-runtime:/var/lib/hagency
    healthcheck:
      test: ["CMD", "node", "-e", "fetch(`http://127.0.0.1:$${process.env.HAGENCY_BACKEND_PORT || 8090}/health`).then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"]
      interval: 5s
      timeout: 3s
      retries: 12
      start_period: 10s
    stop_grace_period: 15s

  dashboard:
    image: "${HAGENCY_IMAGE:-hagency-services:local}"
    build:
      context: ..
      dockerfile: services/Dockerfile
    command: ["node", "server.js"]
    restart: unless-stopped
    network_mode: host
    read_only: true
    tmpfs: [/tmp]
    env_file:
      - ${HAGENCY_ENV_FILE:-../.env}
    environment:
      NODE_ENV: production
      HAGENCY_RUNTIME_DIR: /var/lib/hagency
    volumes:
      - hagency-runtime:/var/lib/hagency
    depends_on:
      backend:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "node", "-e", "fetch(`http://127.0.0.1:$${process.env.HAGENCY_WEB_PORT || 8084}/`).then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"]
      interval: 5s
      timeout: 3s
      retries: 12
      start_period: 5s
    stop_grace_period: 10s

  bridge:
    image: "${HAGENCY_IMAGE:-hagency-services:local}"
    build:
      context: ..
      dockerfile: services/Dockerfile
    command: ["services/run-bridge-container.sh"]
    restart: unless-stopped
    network_mode: host
    pid: host
    read_only: true
    tmpfs: [/tmp]
    env_file:
      - ${HAGENCY_ENV_FILE:-../.env}
    environment:
      NODE_ENV: production
      HAGENCY_RUNTIME_DIR: /var/lib/hagency
    volumes:
      - hagency-runtime:/var/lib/hagency
    depends_on:
      backend:
        condition: service_healthy
    stop_grace_period: 15s

  relay:
    image: "${HAGENCY_IMAGE:-hagency-services:local}"
    build:
      context: ..
      dockerfile: services/Dockerfile
    command: ["node", "push-relay.js"]
    restart: unless-stopped
    network_mode: host
    read_only: true
    tmpfs: [/tmp]
    env_file:
      - ${HAGENCY_ENV_FILE:-../.env}
    environment:
      NODE_ENV: production
      HAGENCY_RUNTIME_DIR: /var/lib/hagency
      PUSH_RELAY_MODE: local
    volumes:
      - hagency-runtime:/var/lib/hagency
    depends_on:
      backend:
        condition: service_healthy
    stop_grace_period: 10s

volumes:
  hagency-runtime:
